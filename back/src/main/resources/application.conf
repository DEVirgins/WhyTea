ktor {
    development = true
    deployment {
        port = 8080
        watch = [classes]
    }

    application {
        modules = [ codes.whytea.ApplicationKt.main ]
    }
}

jwt {
    domain = "http://localhost:8080"
    realm = "TestAuth"
    audience = "User"
}